repositories{
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'gotenna-public-sdk', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
